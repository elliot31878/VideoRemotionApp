import config from '../assets/config.json';

export const colors = config.color;
export const footage = config.footage;
export const startText = config.text.start_text;
export const middleText = config.text.middle_text;
export const information = config.text.information;
export const endtext = config.text.end_text;
export const mainFont = config.main_font;
export const secondaryFont = config.secondary_font;
export const savePath = config.save_path;
export const musicCategory = config.music_category;
export const fps = config.fps;
export const screenSize = config.screen_size;
